# SebbyWebServer
A simple python webserver
# Config
Mostly configurable using the `config.yml`, port, host, certificates, messages.
# Features
 - Functions (currently have to be added in `main.py`)
 - Only requires one pip module (`pyyaml`)
 - Simple and open!
# Planned
 - Loadable custom functions
